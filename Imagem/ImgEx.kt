package com.example.claro.iflix.adapter

class ImgEx
{
    val uri = "wwww.endereco"

    Picasso.with(contextExemplo).load(uri).into(imageViewExemplo)

    //implementation 'com.squareup.picasso:picasso:2.5.2'
}